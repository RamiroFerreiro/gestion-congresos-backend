# Congreso Backend

Backend del sistema de gestión de congresos científicos desarrollado con Spring Boot.

## Configuración

Crear un archivo `.env` con las siguientes variables:

```env
DB_URL=
DB_USERNAME=
DB_PASSWORD=
```

Luego ejecutar:

```bash
mvn spring-boot:run
```
